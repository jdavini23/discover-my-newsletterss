{
  "version": 2,
  "buildCommand": "npm run vercel-build",
  "outputDirectory": "dist/component-library",
  "framework": "create-react-app",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "env": {
    "NODE_OPTIONS": "--max_old_space_size=4096"
  }
}
